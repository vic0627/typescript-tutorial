"use strict";
const forever = () => {
    while (true) {
        //...
    }
};
const ran = Math.random();
const ifForever = (n) => {
    while (n >= 0.5) {
        //...
    }
};
ifForever(ran);
ifForever(0.1);
ifForever(0.6);
